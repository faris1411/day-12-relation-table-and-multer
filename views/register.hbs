<!DOCTYPE html>
<html>
<head>
  <title>Home</title>
  <link rel="stylesheet" href="./assets/style/style.css">
  <!-- Bootstrap -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css">
  <!-- Font Awesome -->
  <script src="https://kit.fontawesome.com/7de6e485c3.js" crossorigin="anonymous"></script>
</head>
  <body>
    <nav class="navbar navbar-expand-lg bg-light navbar-custom">
      <div class="container">
        <!-- Left -->
        <a class="navbar-brand me-5" href="/">
          <img src="./assets/img/logo_dw.png" alt="">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link" href="/">Home</a>
            </li>
          </ul>
          <!-- End Left -->
          <!-- Right -->
          <ul class="navbar-nav ms-auto mb-2 mb-lg-0 me-5">
            <li class="nav-item">
              <a class="nav-link active-custom" href="/register">Register</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/login">Login</a>
            </li>
          </ul>
          <a class="btn btn-dark" href="/contact">Contact Me</a>
          <!-- End Right -->
        </div>
      </div>
    </nav>
    <div class="content-container d-flex justify-content-center align-items-center">
      <form class="row g-3 w-25" action="/register" method="post">
        <h4 class="text-center fw-bold mb-4">REGISTER YOUR ACCOUNT</h4>
        {{#if messages.error}}
        <div class="alert alert-warning" role="alert">
          {{messages.error}}
        </div>
        {{/if}}
        <div class="col-12">
          <label for="user-name" class="fw-semibold">Name</label>
          <input type="text" class="form-control shadow" id="user-name" name="name">
        </div>
        <div class="col-12">
          <label for="user-email" class="fw-semibold">Email</label>
          <input type="text" class="form-control shadow" id="user-email" name="email">
        </div>
        <div class="col-12">
          <label for="user-password" class="fw-semibold">Password</label>
          <input type="password" class="form-control shadow" id="user-password" name="password">
        </div>
        <div class="d-flex justify-content-end mt-5">
          <button type="submit" class="btn btn-dark btn-oval">Submit</button>
        </div>
      </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>